# Rate limiting zones
limit_req_zone $binary_remote_addr zone=bootnode_limit:10m rate=20r/s;
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

# Connection limiting (optional but recommended for bootnodes)
limit_conn_zone $binary_remote_addr zone=conn_limit:10m;